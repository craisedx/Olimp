@model List<Olimp.ViewModels.StoreWarehouse.StoreWarehouseViewModel>

<div class="container mt-5">
    <div class="row mb-5 d-flex justify-content-center">
        <form method="post" asp-action="AllProducts">
            <div class="col-lg-12 d-flex justify-content-center flex-column">
                <div class="form-group">
                    <p style="text-align: center" class="h3 hb"> Категория</p>
                    <select size="1" name="categoriesId" class="form-control">
                        <option value="0">Не выбрано</option>
                        @foreach (var item in ViewBag.Categories)
                        {
                            <option value="@item.Id">@item.ProductCategory</option>
                        }
                    </select>
                </div>
                <button type="submit" class="btn rounded shadow-sm rounded-pill hr" style="background-color: #FFC23A; font-size: 20px">Посмотреть</button>
            </div>
        </form>


    </div>
    <div class="row">
        @foreach (var item in @Model)
            {
                <div class="col-4 mb-4">
                    <div class="card">
                        <img class="card-img" src="@item.Product.Image" alt="@item.Product.Title image">
                        <div class="card-body">
                            <h4 class="card-title">@item.Product.Title</h4>
                            <div class="buy d-flex justify-content-between align-items-center">
                                <div class="price text-success">
                                    <h5 class="mt-4">@item.Price BYN</h5>
                                </div>
                                <a asp-action="Product" asp-controller="Product" asp-route-id="@item.Id" class="btn btn-primary mt-3"><i class="fas fa-shopping-cart"></i>Подробнее</a>
                            </div>
                        </div>
                    </div>
                </div>
            }
    </div>
</div>